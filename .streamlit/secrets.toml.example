# 範本:複製成 .streamlit/secrets.toml(本地)或貼到 Streamlit Cloud 的 Secrets 欄。
# 真正的 secrets.toml 已被 .gitignore 排除,不會上傳。

OPENAI_API_KEY = "sk-proj-你的金鑰"

# 上線保護:設了密碼後,網頁需輸入此密碼才能使用(本地不設則直接放行)。
APP_PASSWORD = "改成你自己的密碼"
